---
import type { Category } from '../../types/types'


type Props = {
    category: Category
}

const icons: Record<string, string>  = {
    king: '👑',
    fighter: '🗡',
    adviser: '🎓',
    squire: '🛡'
}

const {category} = Astro.props
---


<i class="emoji">{ icons[category] }</i>

<style>
    i {
        font-style: normal;
        position: absolute;
        right: 5px;
        top: 5px;
    }
</style>